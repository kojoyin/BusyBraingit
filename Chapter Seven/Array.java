
public class Array {

	public static void main(String[] args) {
		//Method Declaration of an array 
		//int [] arrayRefVar; // the preferred way
		//int  arrayRefVar []; //this works too not for java but for c/c++
		
		double[] myList = {1.9, 2.9, 3.4, 3.5}; // preferred way
	// or
		//double myList []; //this works too not for java but for c/c++
		
		// Method of creating Arrays
		//One can create an Array by using the new operator with the following Syntax
		
		//arrayRefVar = new int [arraySize];
	  //this above statement does two things-
	// it creates an array using new dataType[arraySize].
	// it assigns the reference of the newly created array to the variable arrayRefVar 
		
    
	}

}
